export type UserType = {
  id: string;
  email: string;
  passwordHash: string;
  name: string;
  nationality: string;
  telephoneNumber: string;
  province: string;
  district: string;
  ward: string;
  street: string;
};
